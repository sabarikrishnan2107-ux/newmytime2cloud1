# PyInstaller build artifacts (regenerated by build-face.bat)
build/
dist/

# Staged InsightFace model pack (~330 MB) copied from ~/.insightface by
# build-face.bat — never committed, bundled into the exe at build time.
models/

__pycache__/
*.pyc
