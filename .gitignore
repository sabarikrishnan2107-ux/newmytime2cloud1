# Camera service AI model files (large binary, download separately)
camera-service/models/models/
*.onnx
*.zip

# Environment files (NEVER commit secrets)
.env
.env.live
.env.local
.env.production
.env.development
.env.staging
**/.env
**/.env.live
**/.env.local
**/.env.production
**/.env.development
**/.env.staging

# SSH / server keys (NEVER commit — these grant server access)
*.ppk
*.pem
*_key
*_key.*
new_server_key.ppk
ssh_login_key.ppk

# OS
.DS_Store
Thumbs.db
