{
  "name": "mytime2cloud-push-service",
  "version": "1.0.0",
  "private": true,
  "description": "Local SSE push relay for the desktop build (replaces external v2push). Zero dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  }
}
