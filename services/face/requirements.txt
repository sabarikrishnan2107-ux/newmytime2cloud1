# FILE: backend/requirements.txt
# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# AI / Face Recognition
mediapipe==0.10.14         # /validate-passport face detection (was missing — main.py imports it)
insightface==0.7.3
onnxruntime==1.17.3        # CPU-only (use onnxruntime-gpu for GPU)
opencv-python-headless==4.9.0.80
numpy==1.26.4
Pillow==10.3.0
